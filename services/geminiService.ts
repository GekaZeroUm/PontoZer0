// Serviço de IA desativado.
// Para reativar, instale o SDK do Google GenAI e restaure o código.

export const generateMarketingCopy = async (niche: string, productType: string) => {
  return [];
};